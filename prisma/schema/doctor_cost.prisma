model DoctorCosts {
  id           Int         @id @default(autoincrement())
  doctorId     Int
  baseFee      Decimal     @default(0.0)
  extraFees    Json?
  deletedAt    DateTime?   @map("deleted_at")
  createdAt    DateTime    @default(now()) @map("created_at")
  updatedAt    DateTime    @updatedAt @map("updated_at")



  @@map("doctor_costs") 
}
