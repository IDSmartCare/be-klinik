model MasterSubjective {
  id          Int      @id @default(autoincrement())
  idFasyankes Int
  text        String
  createdBy   DateTime
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}
